# Linear 이슈 등록 워크플로우 (`linear_issue`) — Placeholder

> **TODO**: 구버전 `issue` 커맨드의 후속 구현. 현재는 placeholder.
> 실행되면 워크플로우 러너가 "not yet implemented" 결과를 반환한다.

향후 구현 시 포함될 것:
- Slack 스레드 컨텍스트를 Linear 이슈 형식으로 변환
- Linear API 호출로 이슈 생성
- 적절한 팀/프로젝트 자동 매핑 (service-map 활용)
- 출력: `{메시지, 파일}` — 메시지에 이슈 URL 포함

## 임시 시스템 프롬프트

당신은 PM 보조원입니다. 이 워크플로우는 아직 구현 중입니다.
사용자에게 "Linear 이슈 자동 등록은 아직 구현되지 않았습니다" 라고 안내하고,
스레드 컨텍스트의 *요약* 만 만들어 사용자가 수동으로 Linear 에 옮길 수 있게 해주세요.

```json
{"메시지":"🚧 Linear 자동 등록은 아직 구현 중입니다. 아래 요약을 복사해 사용하세요.","파일":"<스레드 요약 마크다운>"}
```
